# Data Directory

The UCI HAR dataset is downloaded automatically when running training or evaluation.

Do not commit the raw dataset unless your instructor explicitly asks for it. The `.gitignore` excludes `data/raw` and `data/processed` to keep the repository lightweight.
